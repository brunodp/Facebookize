begin
  require "jeweler"
  Jeweler::Tasks.new do |gem|
    gem.name = "facebookize"
    gem.summary = "Setup Rails for Facebook App development"
    gem.files = Dir["{lib}/**/*", "{app}/**/*", "{config}/**/*"]
    # other fields that would normally go in your gemspec
    # like authors, email and has_rdoc can also be included here
    gem.version = '0.0.1'
    gem.email = 'brunodp@gmail.com'
    gem.description = 'Setup Rails for Facebook App development'
    gem.author = 'Bruno Di Pentima'
    gem.homepage = 'https://github.com/brunodp/Facebookize'

  end
rescue
  puts "Facebookize or one of its dependencies is not installed."
end